<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/antd@5.0.0/dist/reset.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/antd@5.0.0/dist/antd.min.css" rel="stylesheet">
    <style>
        .admin-layout {
            min-height: 100vh;
            background: #f0f2f5;
        }
        .admin-header {
            background: #001529;
            color: white;
            padding: 0 24px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 64px;
        }
        .admin-content {
            padding: 24px;
            margin: 24px;
            background: white;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        .stats-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 24px;
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 16px;
            margin-bottom: 24px;
        }
        .stat-item {
            background: rgba(255,255,255,0.1);
            padding: 16px;
            border-radius: 6px;
            text-align: center;
        }
        .stat-number {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 4px;
        }
        .stat-label {
            font-size: 14px;
            opacity: 0.9;
        }
    </style>
</head>
<body>
    <div class="admin-layout">
        <!-- Header -->
        <div class="admin-header">
            <h2 style="margin: 0; color: white;">🗑️ <%= title %></h2>
            <div>
                Xin chào, <strong><%= user?.email || 'Admin' %></strong>
            </div>
        </div>

        <!-- Content -->
        <div class="admin-content">
            <!-- Stats Overview -->
            <div class="stats-card">
                <h3 style="margin-top: 0; color: white;">📊 Tổng quan items đã xóa</h3>
                <div class="stats-grid" id="statsGrid">
                    <!-- Stats will be loaded here -->
                </div>
            </div>

            <!-- Soft Delete Manager Component -->
            <div id="soft-delete-manager">
                <!-- React component will be mounted here -->
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/antd@5.0.0/dist/antd.min.js"></script>
    
    <script>
        // Load stats
        async function loadStats() {
            try {
                const token = localStorage.getItem('token')
                const response = await fetch('/api/v1/admin/soft-delete-stats', {
                    headers: {
                        'Authorization': `Bearer ${token}`
                    }
                })
                
                if (response.ok) {
                    const result = await response.json()
                    displayStats(result.data)
                }
            } catch (error) {
                console.error('Error loading stats:', error)
            }
        }

        function displayStats(stats) {
            const statsGrid = document.getElementById('statsGrid')
            const modelLabels = {
                products: '📦 Sản phẩm',
                users: '👥 Người dùng',
                categories: '🏷️ Danh mục',
                brands: '🏢 Thương hiệu',
                blogs: '📝 Blog',
                contacts: '📧 Liên hệ',
                reviews: '⭐ Đánh giá',
                wishlists: '❤️ Yêu thích'
            }

            statsGrid.innerHTML = Object.entries(stats).map(([key, value]) => `
                <div class="stat-item">
                    <div class="stat-number">${value}</div>
                    <div class="stat-label">${modelLabels[key] || key}</div>
                </div>
            `).join('')
        }

        // Simple React-like component for demonstration
        function initializeSoftDeleteManager() {
            const container = document.getElementById('soft-delete-manager')
            
            // Create a simple interface
            container.innerHTML = `
                <div style="background: white; padding: 24px; border-radius: 8px; border: 1px solid #d9d9d9;">
                    <h3>🔧 Quản lý Xóa Mềm</h3>
                    <p>Sử dụng các API endpoints sau để quản lý items đã xóa:</p>
                    
                    <div style="background: #f6f8fa; padding: 16px; border-radius: 6px; margin: 16px 0;">
                        <h4>📡 API Endpoints:</h4>
                        <ul style="margin: 0; padding-left: 20px;">
                            <li><code>GET /api/v1/soft-delete/{model}</code> - Lấy danh sách items đã xóa</li>
                            <li><code>PATCH /api/v1/soft-delete/{model}/{id}/restore</code> - Khôi phục item</li>
                            <li><code>DELETE /api/v1/soft-delete/{model}/{id}/permanent</code> - Xóa vĩnh viễn</li>
                            <li><code>PATCH /api/v1/soft-delete/{model}/bulk/restore</code> - Khôi phục hàng loạt</li>
                            <li><code>DELETE /api/v1/soft-delete/{model}/bulk/permanent</code> - Xóa vĩnh viễn hàng loạt</li>
                        </ul>
                    </div>

                    <div style="background: #e6f7ff; padding: 16px; border-radius: 6px; border-left: 4px solid #1890ff;">
                        <h4>🎯 Supported Models:</h4>
                        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 8px;">
                            <span>📦 products</span>
                            <span>👥 users</span>
                            <span>🏷️ categories</span>
                            <span>🏢 brands</span>
                            <span>📝 blogs</span>
                            <span>📧 contacts</span>
                            <span>⭐ reviews</span>
                            <span>❤️ wishlists</span>
                        </div>
                    </div>

                    <div style="margin-top: 16px;">
                        <button onclick="window.open('/api/v1/admin/soft-delete', '_blank')" 
                                style="background: #1890ff; color: white; border: none; padding: 8px 16px; border-radius: 4px; cursor: pointer;">
                            🚀 Mở Dashboard Đầy Đủ
                        </button>
                    </div>
                </div>
            `
        }

        // Initialize on page load
        document.addEventListener('DOMContentLoaded', function() {
            loadStats()
            initializeSoftDeleteManager()
        })
    </script>
</body>
</html>